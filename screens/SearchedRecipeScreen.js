import {
  StyleSheet,
  View,
  ImageBackground,
  ScrollView,
  TouchableOpacity,
} from "react-native";
export default function SearchedRecipeScreen({ navigation }) {
  return <View style={{ backgroundColor: "red", flex: 1 }} />;
}

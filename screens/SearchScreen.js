import {
  StyleSheet,
  View,
  ImageBackground,
  ScrollView,
  TouchableOpacity,
} from "react-native";
export default function SearchScreen({ navigation }) {
  return <View style={{ backgroundColor: "red", flex: 1 }} />;
}

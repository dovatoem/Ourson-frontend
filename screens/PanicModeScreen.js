import {
  StyleSheet,
  View,
  ImageBackground,
  ScrollView,
  TouchableOpacity,
} from "react-native";
export default function PanicModeScreen({ navigation }) {
  return <View style={{ backgroundColor: "red", flex: 1 }} />;
}

import {
  StyleSheet,
  View,
  ImageBackground,
  ScrollView,
  TouchableOpacity,
} from "react-native";
export default function RegenerateFavScreen({ navigation }) {
  return <View style={{ backgroundColor: "red", flex: 1 }} />;
}

import {
  Chip,
  useTheme,
  Button,
  Dialog,
  Portal,
  Text,
  Divider,
  RadioButton,
} from "react-native-paper";
export default function Day7SundayScreen({ navigation }) {
  return <Text>Dimanche</Text>;
}
